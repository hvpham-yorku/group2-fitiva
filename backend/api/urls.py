from django.urls import path
from rest_framework.routers import DefaultRouter
from . import views

# Placeholder for now - we can add actual endpoints later
urlpatterns = [
    # API endpoints will go here in ITR1
]
